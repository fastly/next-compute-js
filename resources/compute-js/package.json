{
    "main": "src/index.js",
    "name": "next-compute-js",
    "private": true,
    "license": "MIT",
    "scripts": {
        "build": "js-compute-runtime bin/index.js bin/main.wasm",
        "deploy": "npm run build && fastly compute deploy",
        "prebuild": "npx @fastly/compute-js-static-publish --build-static && webpack"
    },
    "dependencies": {
        "@fastly/js-compute": "^0.3.0"
    },
    "devDependencies": {
        "@fastly/compute-js-static-publish": "^1.0.0",
        "@fastly/next-compute-js": "^0.1.0",
        "buffer": "^6.0.3",
        "core-js": "^3.19.1",
        "path-browserify": "^1.0.1",
        "process": "^0.11.10",
        "react": "^17.0.2",
        "react-dom": "^17.0.2",
        "url": "^0.11.0",
        "webpack": "^5.64.0",
        "webpack-cli": "^4.9.1"
    },
    "engines": {
        "node": "^16"
    }
}
